#pragma once
#include <iostream>
#include <map>
#include <string>

std::multimap<std::string, std::string> containers;		// container for included messages local
std::multimap<std::string, std::string> allContainers;
SOCKET connection;